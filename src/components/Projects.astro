---
// Dot coordinates as % of image width/height
const locations = [
  {
    id: "italy",
    label: "Italy",
    x: 62,
    y: 37,
    projects: [
      { name: "EICMA", dates: "Milano", place: "Italy" },
      { name: "COSMOPROF", dates: "Bologna", place: "Italy" },
      { name: "COSMOPACK", dates: "Bologna", place: "Italy" },
      { name: "MIDO", dates: "Milan", place: "Italy" },
      { name: "SALONE DEL MOBILE", dates: "Milan", place: "Italy" },
      { name: "MACEF", dates: "Milano", place: "Italy" },
      { name: "WHITE SNOW", dates: "Milan", place: "Italy" },
      { name: "HOST", dates: "Milan", place: "Italy" },
      { name: "UNICA", dates: "Milan", place: "Italy" },
      { name: "LINEAPELLE", dates: "Milan", place: "Italy" },
      { name: "TUTTO FOOD", dates: "Milano", place: "Italy" },
      { name: "SAPORI DELLA COREA", dates: "Milano", place: "Italy" },
      { name: "KOSHI KOBA", dates: "Milano", place: "Italy" },
      { name: "VENDITALIA", dates: "Milan", place: "Italy" },
      { name: "WUWHS", dates: "Florence", place: "Italy" },
      { name: "ISUOG", dates: "Rome", place: "Italy" },
      { name: "EAO", dates: "Rome, Milan", place: "Italy" },
      { name: "HYOSUNG", dates: "Milano", place: "Italy" },
      { name: "HYUNDAI", dates: "Milano", place: "Italy" },
      { name: "KOREAN AIRLINE", dates: "Milano", place: "Italy" },
      { name: "ENERSOL", dates: "Milano, Verona", place: "Italy" },
      { name: "BI-MU", dates: "Milano", place: "Italy" },
      { name: "MCE", dates: "Milano", place: "Italy" },
      { name: "ESCRS", dates: "Copenhagen, Milan, London, Vienna", place: "Italy" },
      { name: "SLOWFOOD", dates: "Turin", place: "Italy" },
    ],
  },
  {
    id: "usa",
    label: "America",
    x: 8,
    y: 45,
    projects: [
      { name: "PREMIERE VISION", dates: "New York", place: "America" },
      { name: "OUTDOOR Retailers", dates: "Denver", place: "America" },
    ],
  },
  {
    id: "spain",
    label: "Spain",
    x: 54,
    y: 40,
    projects: [
      { name: "FOTOVOLTAIC", dates: "Madrid", place: "Spain" },
      { name: "CPHI", dates: "Madrid", place: "Spain" },
      { name: "EWMA", dates: "Madrid", place: "Spain" },
      { name: "EAO", dates: "Madrid", place: "Spain" },
      { name: "CCIB", dates: "Barcelona", place: "Spain" },
      { name: "EASD", dates: "Lisbon", place: "Spain" },
    ],
  },
  {
    id: "france",
    label: "France",
    x: 60,
    y: 33,
    projects: [
      { name: "SILMO", dates: "Paris", place: "France" },
      { name: "PREMIER VISION", dates: "Paris", place: "France" },
      { name: "ADF", dates: "Paris", place: "France" },
      { name: "MAISON OBJECT", dates: "Paris", place: "France" },
      { name: "EUROSPINE", dates: "Lyon, Dublin", place: "France" },
      { name: "AMWC", dates: "Montecarlo", place: "France" },
    ],
  },
  {
    id: "germany",
    label: "Germany",
    x: 72,
    y: 35,
    projects: [
      { name: "EUROBIKE", dates: "Friedrichshafen", place: "Germany" },
      { name: "AGRITECH", dates: "Hannover", place: "Germany" },
      { name: "ELECTRONICA", dates: "Munich", place: "Germany" },
      { name: "INTERSOLAR", dates: "Munich", place: "Germany" },
      { name: "TUBE", dates: "Dusseldorf", place: "Germany" },
      { name: "MEDICA", dates: "Dusseldorf", place: "Germany" },
      { name: "IDS", dates: "Köln", place: "Germany" },
      { name: "A+A", dates: "Dussedorf", place: "Germany" },
      { name: "FA", dates: "Frankfurt", place: "Germany" },
      { name: "CPHI", dates: "Frankfurt", place: "Germany" },
      { name: "BOOKFAIR", dates: "Leipzig", place: "Germany" },
      { name: "EUROPERIO", dates: "Amsterdam", place: "Germany" },
      { name: "DENTIUM World Forum", dates: "Budapest", place: "Germany" },
      { name: "EWMA", dates: "Netherlands", place: "Germany" },
      { name: "IECR", dates: "Vienna", place: "Germany" },
    ],
  },
  {
    id: "russia",
    label: "Russia",
    x: 90,
    y: 30,
    projects: [
      { name: "INTERCHARM", dates: "Moscow", place: "Russia" },
    ],
  },
];

import { Image } from "astro:assets";
import mapImage from "../assets/images/map.png";
---

<section class="relative mx-auto max-w-[1336px] px-8 py-32 text-white md:px-16">
  <projects-map class="flex flex-col md:flex-row-reverse md:justify-between md:items-start">
    <div class="relative mx-auto aspect-[1858/2304] w-full md:w-1/2">
      <Image
        src={mapImage}
        alt="World map showing project locations"
        class="absolute inset-0 h-full w-full object-contain"
      />

      {
        locations.map((loc) => (
          <button
            type="button"
            class="map-dot absolute h-[20px] w-[20px] -translate-x-1/2 -translate-y-1/2 cursor-pointer rounded-full bg-[#ffa500] transition-transform duration-150 hover:scale-125 focus:outline-none focus-visible:ring-2 focus-visible:ring-white"
            style={`left:${loc.x}%; top:${loc.y}%`}
            data-id={loc.id}
            aria-label={`View projects in ${loc.label}`}
          />
        ))
      }
    </div>

    <div class="mt-12 md:w-1/2">
      <h2
        id="projects"
        class="projects-heading font-heading text-accent mb-10 text-5xl font-thin"
      >
        Projects
      </h2>

      <div
        class="projects-list mt-2 max-w-[520px] max-h-[520px] overflow-auto text-sm leading-relaxed text-white/70"
      >
        <p data-default-text class="text-2xl">
          Select a location on the map to view the projects associated with it.
        </p>
      </div>
    </div>

    <script
      type="application/json"
      data-locations
      set:html={JSON.stringify(locations)}
    />
  </projects-map>
</section>

<style>
  .map-dot.is-active {
    box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.35);
  }
</style>

<script>
  class ProjectsMap extends HTMLElement {
    private locations: Array<{
      id: string;
      label: string;
      projects: { name: string; dates: string; place: string }[];
    }> = [];
    private listEl!: HTMLElement;
    private dots!: NodeListOf<HTMLButtonElement>;

    connectedCallback() {
      const dataScript = this.querySelector<HTMLScriptElement>(
        "script[data-locations]",
      );
      this.locations = dataScript
        ? JSON.parse(dataScript.textContent || "[]")
        : [];
      this.listEl = this.querySelector(".projects-list")!;
      this.dots = this.querySelectorAll(".map-dot");

      this.dots.forEach((dot) => {
        dot.addEventListener("click", () =>
          this.selectLocation(dot.dataset.id!),
        );
      });
    }

    selectLocation(id: string) {
      this.dots.forEach((d) =>
        d.classList.toggle("is-active", d.dataset.id === id),
      );

      const loc = this.locations.find((l) => l.id === id);
      if (!loc) return;

      this.listEl.innerHTML = `
        <h3 class="mb-8 text-4xl font-thin font-heading text-white">${loc.label}</h3>
        <ul class="space-y-2">
          ${loc.projects
            .map(
              (p) => `
            <li class="border-b border-white/10 pb-2">
              <span class="text-2xl text-white">${p.name}</span>
              <span class="text-2xl text-white/50"> — ${p.dates}, ${p.place}</span>
            </li>`,
            )
            .join("")}
        </ul>
      `;
    }
  }

  customElements.define("projects-map", ProjectsMap);
</script>
